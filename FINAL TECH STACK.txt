1. Framework & Tools
Next.js – React framework that handles routing, pages, and API calls.
Tailwind CSS – for fast, responsive styling.
React Query – handles data fetching, caching, and loading states.

2. Backend
Node.js with Express.js – REST API backend to handle business logic.
Authentication – JSON Web Token (JWT) for login/logout + protecting routes.
CORS middleware – to allow frontend (Next.js) to talk to backend (Express).
Validation library (like express-validator) – for clean input validation.

3. . Database
MySQL – relational DB (since you’ve chosen it).
mysql2 or Sequelize ORM – to connect backend with database. (Start with mysql2 if you want raw SQL queries, Sequelize if you prefer ORM for easier CRUD).
Tables to Start With:
-- Drop and create database
DROP DATABASE IF EXISTS soft_eng;
CREATE DATABASE IF NOT EXISTS soft_eng;
USE soft_eng;
 
-- Roles table
CREATE TABLE roles (
    roleId INT AUTO_INCREMENT PRIMARY KEY,
    roleName VARCHAR(50) NOT NULL UNIQUE
);

-- Users table
CREATE TABLE users (
    userId INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(50) UNIQUE NOT NULL,
    password VARCHAR(255) NOT NULL,
    email VARCHAR(100) UNIQUE NOT NULL,
    firstName VARCHAR(50) NULL,
    lastName VARCHAR(50) NULL,
    contactNumber VARCHAR(20) NULL,
    address VARCHAR(255) NULL,
    roleId INT DEFAULT 3,
    createdAt TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updatedAt TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (roleId) REFERENCES roles(roleId)
);

-- Insert roles
INSERT INTO roles (roleName)
VALUES 
    ('Technician'),
    ('Owner'),
    ('Customer');

-- Insert default users
-- Technician
INSERT INTO users (username, password, email, firstName, lastName, roleId)
VALUES ('Technician', 'tech123', 'tech@gmail.com', 'Mark', 'Reyes', 1);

-- Owner
INSERT INTO users (username, password, email, firstName, lastName, roleId)
VALUES ('Owner', 'owner', 'owner@gmail.com', 'Lawrence', 'Gonzaga', 2);

-- Customer (roleId defaults to 3)
INSERT INTO users (username, password, email, firstName, lastName)
VALUES ('Customer', 'customer', 'customer@gmail.com', 'Jane', 'Smith');

-- Verify data
SELECT * FROM roles;
SELECT * FROM users;


4. Authentication & Security
JWT – for login, logout, and securing routes.
Role-based Access Control (RBAC) – optional but useful if you want admin vs staff.

backend/
│── src/
│   ├── features/
│   │   ├── auth/
│   │   │   ├── auth.routes.js
│   │   │   ├── auth.controller.js
│   │   │   └── auth.model.js
│   │   │
│   │   ├── users/
│   │   │   ├── users.routes.js
│   │   │   ├── users.controller.js
│   │   │   └── users.model.js
│   │   │
│   │   │
│   │   └── ... (other modules)
│   │
│   ├── middlewares/
│   │   ├── authMiddleware.js
│   │   ├── roleMiddleware.js
│   │
│   ├── config/
│   │   └── db.js
│   │
│   ├── utils/
│   │   └── helpers.js
│   │
│──app.js
│──server.js



✅ Step 1: Initialize Project

Inside your backend folder:
npm init -y


This creates a package.json to track dependencies.

✅ Step 2: Install Core Dependencies
Now install the backend tech stack you mentioned:
npm install express cors  jsonwebtoken express-validator dotenv

express → your backend framework
cors → allows frontend (Next.js) to connect to backend
jsonwebtoken (JWT) → authentication system
express-validator → input validation (clean request body checking)
dotenv → load environment variables (secrets, db config, JWT key)

✅ Step 3: Install Dev Dependencies (optional but recommended)
npm install --save-dev nodemon

nodemon restarts the server automatically whenever you save changes.

✅ Step 4: Setup Scripts in package.json
In package.json, add this inside "scripts":

"scripts": {
  "start": "node server.js",
  "dev": "nodemon server.js"
}
Now you can run:
npm run dev

✅ Step 5: Create Base Files
server.js → entry point
.env → environment variables (like PORT, JWT_SECRET)
/src/config/db.js → database connection (if using MySQL/Postgres later)
/src/features/auth/ → auth routes, controllers, etc.

👉 Order of setup after installation:
Basic Express server (Hello World API)
CORS middleware setup
Authentication (JWT) — register, login routes
bcrypt hashing for passwords
Protected routes with JWT middleware
Validation (express-validator) for clean inputs